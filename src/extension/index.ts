/**
 * Extension module exports
 *
 * This module provides components for browser extension use.
 * Entry points (sidepanel.tsx, sidepanel-minimal.tsx) are loaded directly by HTML files.
 */

// Reusable chat component for sidepanel
export { default as MinimalChat } from './MinimalChat'
